#!/usr/bin/env node

/**
 * Oply CLI — Entry Point
 * 
 * Usage:
 *   oply                  # Interactive mode
 *   oply init             # Initialize project
 *   oply status           # Pipeline status
 *   oply deploy           # Trigger deployment
 *   oply logs             # View logs
 *   oply ai-debug         # AI debugging assistant
 *   oply pipeline         # Pipeline management
 *   oply docker           # Docker operations
 *   oply k8s              # Kubernetes operations
 *   oply rollback         # Rollback deployment
 */

import '../src/index.js';
